# IntelIMC Arrow Lake validation report

Generated by `Deploy\Build-ARLValidation.ps1` on a real ARL machine.

Captured: 2026-05-02 12:15:13+02:00
Host OS:  Windows 10.0.26200.0

## Hardware
- CPU: Intel(R) Core(TM) Ultra 9 285K
- Description: Intel64 Family 6 Model 198 Stepping 2
- CPUID Family/Model/Stepping: 0x06 / 0xC6 / 0x02
- Module-resolved platform tag: 2 (ARL)

### DRAM modules (SMBIOS via Win32_PhysicalMemory)
| Manufacturer | Part | Capacity | Speed | ConfiguredClockSpeed |
|---|---|---|---|---|
| G Skill Intl | F5-7800J3646H16G  | 16,384 MiB | 7800 MT/s | 7800 MT/s |
| G Skill Intl | F5-7800J3646H16G  | 16,384 MiB | 7800 MT/s | 7800 MT/s |

## Artefact hashes (SHA-256)
- Loaded driver `PawnIO.sys`: `3D830254ACADBF3255F588DBEE48355D241FECB786D270AB43C4B93BB89BADCE`
- `IntelIMC.amx`              : `1808F0A2ED866C9081F37D90FFB8FE3A229EA8496E088367D75B8C12318511E3`
- `IntelIMC.signed.amx`       : `874F21A579C9BE1FBE9C11F598F368004DD4BB04C0D72143DB99CEED326FEE58`

## Diagnostic IOCTL -- `ioctl_read_imc_clock_dbg`
Single call, decodes every intermediate value the static IOCTL uses.

```
  ABI version          : 1
  CPUID FMS            : family=0x06 model=0xC6 stepping=0x02
  Platform tag         : 2 (ARL)
  Source tag           : 1 (MCHBAR_MEMSS_PMA)
  MCHBAR low  (raw)    : 0xFEDC0001
  MCHBAR high (raw)    : 0x00000000
  MCHBAR base (masked) : 0x00000000FEDC0000
  MEMSS_PMA raw        : 0x00000075  (ratio=117, gear_bit=0, reserved=0x00000000)
  SA_PERF raw          : 0x00000000  (ratio=0, refbit=0)
  Failing step         : 0 (OK)
```

## Static IOCTL -- `ioctl_read_imc_clock` (locked max)
10 successive samples (every 100 ms). Stable across samples: **yes**.

| # | HRESULT | Source | Ratio | Ref | Gear | Raw | Flags |
|---|---|---|---|---|---|---|---|
| 1 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 2 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 3 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 4 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 5 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 6 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 7 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 8 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 9 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |
| 10 | 0x00000000 | 1 (MCHBAR_MEMSS_PMA) | 117 | 1 (BCLK/3) | 2 (Gear2) | 0x00000075 | 0x1 |

### Decoded sample 1 (gear-aware formula)

- QCLK       = ratio x ref = 117 x 33.333 MHz = **3,900.000 MHz**
- Data rate  = QCLK x gear = QCLK x 2 = **7,800 MT/s** (DDR5-7800)
- IO clock   = data_rate / 2 = QCLK x gear / 2 = **3,900 MHz**  (= HWiNFO/CPU-Z "Memory Clock")

Note: `data_rate = QCLK * gear` is the gear-aware general form. The PTL script hardcodes `QCLK * 2 * 2`, which is correct only for Gear4 -- on Gear2 hardware (typical Arrow Lake) it overestimates by 2x and reports a non-existent DDR5 speed grade.

## Live IOCTL -- `ioctl_read_imc_clock_live` (workpoint)
The live IOCTL path **is** implemented for Core Ultra in the module via `SA_PERF_STATUS` at `MCHBAR+0x5918`, but on Arrow Lake-S that register reads as `0x00000000` even with SAGV actively transitioning the controller (verified on this 285K with HWiNFO Memory Clock observed swinging 2,400-3,900 MHz under load). The ADL/RPL/PTL register layout does not survive into ARL-S, or the register is not populated by the SoC's power management at this offset.

`IntelIMC.p` therefore short-circuits `ioctl_read_imc_clock_live` to `STATUS_NOT_SUPPORTED` on `PLAT_ARL` until a working live source is identified -- Intel PMT (the existing `IMC_SRC_PMT_QCLK_STATUS` enum) is the most likely candidate, since HWiNFO's live Memory Clock has to come from somewhere and the documented MMIO path does not yield it. The static `MEMSS_PMA` path keeps reporting the locked-max correctly.

Single call result: HRESULT = 0x80070032, return_count = 0

## Cross-validation against external references

External cross-check via HWiNFO64 v8.32 -- see [`screenshots/ARL_hwinfo_memory_clocks.png`](../screenshots/ARL_hwinfo_memory_clocks.png) for the captured Sensors view.

| Source                                    | Reported value                            | Notes |
|---|---|---|
| Module static IOCTL                       | 3,900 MHz IO clock (7,800 MT/s)           | derived as `QCLK x gear` (Gear2) |
| HWiNFO64 Memory Clock                     | 3,899.6 / 3,899.8 / 3,900.6 MHz (min/cur/max) | matches module IO clock to within sub-MHz jitter |
| HWiNFO64 Memory Clock Ratio               | 39.00x                                    | 39 = 117 / 3 (HWiNFO ratio is vs BCLK; module ratio is vs BCLK/3) |
| HWiNFO64 Gear Mode                        | 2                                         | matches module's `Gear2` decode |
| Win32_PhysicalMemory.ConfiguredClockSpeed | 7,800 MT/s (per DIMM)                     | matches module data rate |
| EXPO kit name `G.Skill F5-7800J3646H16G`  | DDR5-7800 nominal                         | matches module data rate |

Four independent references -- the module's own `MCHBAR_MEMSS_PMA` decode, HWiNFO64's Memory Clock sensor (incl. matching Gear Mode), the SMBIOS DRAM data the firmware publishes, and the EXPO kit's nominal speed grade -- all agree on **DDR5-7800, IO clock 3,900 MHz, Gear2**. The gear-aware formula `data_rate = QCLK * gear` reproduces this exactly; the legacy `QCLK * 2 * 2` shortcut from `Test-IntelIMC.ps1` would have predicted DDR5-15600 and disagreed with every reference, confirming that path is incorrect for non-Gear4 hardware.

Based on this four-way agreement, `is_platform_validated` in `IntelIMC.p` was extended to include `PLAT_ARL` alongside `PLAT_PTL`. The static IOCTL therefore no longer sets the `EXPERIMENTAL` bit on Arrow Lake -- the `Flags` column above reads `0x1` (`STATIC_LOCKED`) instead of the previous `0x5` (`STATIC_LOCKED | EXPERIMENTAL`). Consumers can expose the sensor as a primary value on ARL without the preview/experimental label.

## Reproducibility

```powershell
# from an elevated PowerShell at the repo root, with the unrestricted
# PawnIO driver loaded and IntelIMC.signed.amx present:
.\Deploy\Build-ARLValidation.ps1
```

The script is deterministic on ARL: the static MEMSS_PMA line is locked at MRC and does not change at runtime, and the live IOCTL is short-circuited to STATUS_NOT_SUPPORTED for the reasons explained in the Live IOCTL section above. Re-running on the same machine should produce identical values; the locked-max line MUST stay the same.
